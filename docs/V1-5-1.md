# AI-DA Release V1.5.1

**Fecha:** 2026-06-27  
**Modelo:** AI-DA V1.5.1  
**Producción cPanel:** [allinremodeling.us/ai/](https://allinremodeling.us/ai/)

---

## Qué incluye V1.5.1

| Área | Detalle |
|------|---------|
| **Orquestador** | Anthropic Claude (prioridad) → fallback OpenAI; historial conversacional en el LLM |
| **Tono** | Respuestas naturales; reconoce refinamientos (“waterfall”, “cambia a granito”) |
| **Card 1 análisis** | Texto enriquecido server-side (visión + mensaje + web) si GPT devuelve bloque vacío |
| **Card 2 inspiración** | Imagen externa inyectada server-side; rotación por consulta (Tavily → Unsplash) |
| **Card 3 recomendación** | Portfolio All In rotativo; imagen distinta a la de inspiración |
| **Auth cPanel** | Confirmación por correo → `/ai/` con sesión PKCE activa |
| **SmartSlab UI** | Card ancho completo; imágenes sin recorte |
| **Visión** | OpenAI Vision (`gpt-4o-mini`) con personalidad AI-DA |

---

## Bundle cPanel (V1.5.1)

Generar y subir a `public_html/ai/`:

```powershell
npm run deploy:cpanel
# → deploy-cpanel-ai.zip (gitignored — regenerar en cada release)
```

| Verificar en DevTools | Valor V1.5.1 |
|----------------------|--------------|
| JS | `index-C3qpciC7.js` |
| CSS | `index-DPOOnIVn.css` |

**Supabase Auth → URL Configuration**

- Site URL: `https://allinremodeling.us/ai/`
- Redirect URLs: `https://allinremodeling.us/ai/**`

---

## Secrets Supabase

| Secret | Uso |
|--------|-----|
| `ANTHROPIC_API_KEY` | Recomendado — orquestador principal (Claude) |
| `ANTHROPIC_CHAT_MODEL` | Opcional — default `claude-sonnet-4-20250514` |
| `OPENAI_API_KEY` | **Obligatorio** — visión + fallback orquestador |
| `TAVILY_API_KEY` | Inspiración web en vivo (Houzz/Pinterest) |
| `REPLICATE_API_TOKEN` | Edición de fotos (opcional) |

```powershell
npm run deploy:chat
npm run verify:secrets
```

---

## Archivos clave

| Archivo | Rol |
|---------|-----|
| `supabase/functions/_shared/orchestrator.ts` | 5 cards + análisis enriquecido + rotación imágenes |
| `supabase/functions/_shared/trends.ts` | Tavily + fallbacks Unsplash variados |
| `supabase/functions/_shared/portfolio.ts` | Portfolio All In con rotación por seed |
| `src/lib/authCallback.ts` | Confirmación email → sesión |
| `src/lib/authRedirect.ts` | Redirect producción `/ai/` |
| `src/components/DesignBlocks.tsx` | UI cards + fallback análisis vacío |
| `scripts/deploy-cpanel.ps1` | Build con `VITE_AUTH_REDIRECT_URL` embebido |

---

## Deploy checklist

1. `npm run deploy:chat` — backend Supabase
2. `npm run deploy:cpanel` — ZIP frontend
3. Subir ZIP a cPanel → `public_html/ai/` → Extract
4. Hard refresh → confirmar `index-C3qpciC7.js`
5. Probar: consulta con foto + registro con confirmación de correo
